#include<stdio.h>
#include<math.h>

int main()
{
    int i;
    printf("nhap i: ");
    scanf("%d", &i);

    if (i % 2 == 0) {
        printf("so chan");
    }
    else {
        printf("so le");
    }

}